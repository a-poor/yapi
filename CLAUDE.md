
We're using the `bd` CLI for managing tasks. When we're working on a task (e.g. in plan mode), when starting implementing a plan, make sure to start your implementation by marking the task as in-progress. At the end of your implementation, though, don't mark it as done until the user confirms no additional changes need to be made.

